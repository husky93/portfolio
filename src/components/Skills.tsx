import React from 'react';
import styles from '../assets/styles/Skills.module.css';

interface SkillsProps {}

const Skills: React.FC<SkillsProps> = ({}) => {
  return <section id="skills" aria-label="Skills"></section>;
};

export default Skills;
